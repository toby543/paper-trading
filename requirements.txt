requests>=2.31
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
PyYAML>=6.0
tabulate>=0.9
tenacity>=8.2
Flask>=3.0
ruamel.yaml>=0.18
pytest>=8.0

# Optional: only needed if intelligence.enabled is set in config.yaml
# (per-trade LLM rationale). The app runs fine without it -- rationale
# generation is skipped and logged, never a hard error.
anthropic>=0.40

